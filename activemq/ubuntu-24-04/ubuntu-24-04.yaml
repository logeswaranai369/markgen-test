- name: Apache ActiveMQ (Classic) Setup
  hosts: localhost
  tasks:
      - name: Restarting sshd
        shell: "sed -i 's/#Match User anoncvs/ForceCommand echo Please wait until the installation is completed..../g' /etc/ssh/sshd_config && (systemctl restart ssh 2>/dev/null || systemctl restart sshd)"

      - name: Install and configure Apache ActiveMQ (Classic)
        block:
          # On a freshly-booted Ubuntu the apt/dpkg lock is held by cloud-init's boot-time
          # `unattended-upgrades`/apt.systemd.daily. The first package task races it and fails with
          # "Could not get lock /var/lib/dpkg/lock-frontend ... held by process N (unattended-upgr)".
          # STOP the apt-daily timers/services FIRST so they cannot re-acquire the lock mid-run — a
          # one-shot wait isn't enough for long installs (GitLab/BBB/desktop, ~20-40 min): the timer
          # fires again during the install and kills a later apt task with rc=100 (hit live on Ubuntu
          # Desktop). Then block until any in-progress lock clears before installing.
          - name: Stop apt-daily timers/services so they can't grab the dpkg lock mid-install
            shell: |
              systemctl stop apt-daily.timer apt-daily-upgrade.timer 2>/dev/null || true
              systemctl stop apt-daily.service apt-daily-upgrade.service unattended-upgrades.service 2>/dev/null || true
              systemctl kill --kill-who=all apt-daily.service apt-daily-upgrade.service 2>/dev/null || true
              exit 0
            changed_when: false

          - name: Await release of the apt/dpkg lock (boot-time unattended-upgrades)
            shell: |
              for i in $(seq 1 60); do
                fuser /var/lib/dpkg/lock-frontend /var/lib/apt/lists/lock /var/cache/apt/archives/lock >/dev/null 2>&1 || exit 0
                sleep 10
              done
              exit 0
            changed_when: false

          - name: Updating Packages
            ansible.builtin.apt:
              update_cache: yes
              lock_timeout: 300

          - name: Generating activemq_admin_password
            shell: openssl rand -hex 24
            register: activemq_admin_password

          - name: Storing activemq_admin_password
            copy:
              dest: "/home/ubuntu/credentials.txt"
              content: |
                activemq_admin_password = "{{ activemq_admin_password.stdout }}"

          - name: Copy staged config files
            copy:
              src: "{{ item.confsrc }}"
              dest: "{{ item.confdest }}"
            with_items:
              - { confsrc: '/usr/local/src/ubuntu-24-04/etc/systemd/system/activemq.service', confdest: '/etc/systemd/system/'}

          # ----- BEGIN app-specific tasks (LLM-generated from AppSpec) -----
          - name: Wait for apt lock and install Java runtime
            apt:
              name: openjdk-17-jre-headless
              state: latest
              lock_timeout: 300

          - name: Ensure activemq group exists
            group:
              name: activemq
              system: yes

          - name: Ensure activemq system user exists
            user:
              name: activemq
              system: yes
              group: activemq
              home: /opt/activemq
              shell: /usr/sbin/nologin
              create_home: no

          - name: Download Apache ActiveMQ 5.19.10 tarball
            get_url:
              url: https://archive.apache.org/dist/activemq/5.19.10/apache-activemq-5.19.10-bin.tar.gz
              dest: /tmp/apache-activemq-5.19.10-bin.tar.gz
              mode: '0644'

          - name: Check if ActiveMQ is already extracted
            stat:
              path: /opt/activemq/bin/activemq
            register: amq_installed

          - name: Ensure /opt/activemq directory exists
            file:
              path: /opt/activemq
              state: directory
              owner: activemq
              group: activemq
              mode: '0755'

          - name: Extract ActiveMQ to /opt/activemq
            unarchive:
              src: /tmp/apache-activemq-5.19.10-bin.tar.gz
              dest: /opt/activemq
              remote_src: yes
              extra_opts:
                - --strip-components=1
              owner: activemq
              group: activemq
            when: not amq_installed.stat.exists

          - name: Ensure ActiveMQ tree owned by activemq
            file:
              path: /opt/activemq
              state: directory
              owner: activemq
              group: activemq
              recurse: yes

          - name: "Configure web console to bind all interfaces (jetty.xml host 0.0.0.0)"
            replace:
              path: /opt/activemq/conf/jetty.xml
              regexp: '(<property name="host" value=")127\.0\.0\.1("\s*/>)'
              replace: '\g<1>0.0.0.0\g<2>'

          - name: "Add InetAccessHandler include for all networks (0.0.0.0/0)"
            blockinfile:
              path: /opt/activemq/conf/jetty.xml
              marker: "<!-- {mark} MARKGEN INET ACCESS ALL -->"
              insertbefore: '<bean id="invokeStart"'
              block: |
                <bean id="inetAccessIncludeAll" depends-on="inetAccessIncludeLoopbackV6" class="org.springframework.beans.factory.config.MethodInvokingFactoryBean">
                    <property name="targetObject" ref="inetAccessHandler" />
                    <property name="targetMethod" value="include" />
                    <property name="arguments">
                        <list>
                            <value>0.0.0.0/0</value>
                        </list>
                    </property>
                </bean>

          - name: "Make invokeStart depend on the include-all bean so it runs before Server.start()"
            replace:
              path: /opt/activemq/conf/jetty.xml
              regexp: '(<bean id="invokeStart"[^>]*depends-on=")inetAccessIncludeLoopbackV6(")'
              replace: '\g<1>inetAccessIncludeAll\g<2>'

          - name: "Allow unauthenticated access to public welcome pages (securityConstraint authenticate=false)"
            replace:
              path: /opt/activemq/conf/jetty.xml
              regexp: '(<bean id="securityConstraint"[\s\S]*?authenticate" value=")true(")'
              replace: '\g<1>false\g<2>'

          - name: Remove default realm users (admin/admin and user/user)
            lineinfile:
              path: /opt/activemq/conf/jetty-realm.properties
              regexp: '^\s*(admin|user)\s*:'
              state: absent

          - name: "Add admin console user with generated password"
            lineinfile:
              path: /opt/activemq/conf/jetty-realm.properties
              line: "admin: {{ activemq_admin_password.stdout }}, admin"

          - name: Reload systemd to pick up the activemq unit
            systemd:
              daemon_reload: yes

          - name: Enable and start ActiveMQ
            service:
              name: activemq
              state: started
              enabled: yes

          - name: Wait for the web console to serve the welcome page
            uri:
              url: http://localhost:8161/index.html
              status_code: 200
              return_content: yes
            register: amq_web
            until: amq_web.status == 200 and 'ActiveMQ' in (amq_web.content | default(''))
            retries: 30
            delay: 5
          # ----- END app-specific tasks -----

          - name: Creating /opt/cloudstack directory
            ansible.builtin.file:
              path: /opt/cloudstack
              state: directory

          - name: Copy cleanup + update helper scripts
            copy:
              src: "{{ item.confsrc }}"
              dest: "{{ item.confdest }}"
            with_items:
              - { confsrc: '/usr/local/src/ubuntu-24-04/opt/cloudstack/ubuntu-24-04_cleanup.sh', confdest: '/opt/cloudstack/'}
              - { confsrc: '/usr/local/src/ubuntu-24-04/opt/cloudstack/ubuntu-24-04_update.sh', confdest: '/opt/cloudstack/'}

          - name: Wire cleanup script to run on first login
            lineinfile:
              path: /root/.bashrc
              line: "chmod +x /opt/cloudstack/ubuntu-24-04_cleanup.sh && /opt/cloudstack/ubuntu-24-04_cleanup.sh"
              state: present
        always:
          - name: Restarting sshd
            shell: "sed -i 's/ForceCommand echo Please wait until the installation is completed..../#Match User anoncvs/g' /etc/ssh/sshd_config && (systemctl restart ssh 2>/dev/null || systemctl restart sshd)"
